[build]
  publish = "dist"
  command = "mkdir -p dist && cp -r public/* dist/ && cp -r templates dist/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "16"
